[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x"]

[phases.install]
cmds = ["npm install --ignore-engines"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "caddy file-server --root /app/dist --listen :$PORT"
